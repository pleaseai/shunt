# Memory index

- [Default flags -b fails on uncommitted changes](project_default_flags_b_fails_uncommitted.md) — shunt's config `--json -b` returns Bad Request with uncommitted local edits; fall back to plain `cubic review -j`.
